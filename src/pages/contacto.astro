---
import Layout from "../layouts/Layout.astro";
import Formulario from "../components/Formulario.astro";
import DeaContact from "../assets/images/dea3.jpeg";
import SocialIcons from "../components/SocialIcons.astro";
import { Image } from "astro:assets";
---

<Layout
  title="Contacto"
  description="Ponte en contacto con Deanny Bruces para hablar sobre tu proyecto web. Estoy disponible para colaboraciones, preguntas o simplemente para saludar."
  keywords="contacto, Deanny Bruces, colaboración, proyecto web, desarrolladora web, freelance, consultoría"
>
  <section class="contact-section">
    <!-- Background Elements -->
    <div class="contact-background" aria-hidden="true">
      <div class="floating-blob blob-1"></div>
      <div class="floating-blob blob-2"></div>
      <div class="floating-blob blob-3"></div>
      <div class="gradient-orb orb-1"></div>
      <div class="gradient-orb orb-2"></div>
    </div>

    <div class="contact-container content-grid">
      <!-- Hero Section -->
      <div class="contact-hero">
        <div class="hero-badge">
          <svg
            width="16"
            height="16"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
          >
            <path
              d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"
            ></path>
          </svg>
          <span>Hablemos</span>
        </div>

        <h1 class="hero-title">
          Tu próxima web <span class="gradient-text">exitosa</span> comienza
          aquí
        </h1>

        <p class="hero-subtitle">
          Cuéntame tu visión y juntos la convertiremos en una experiencia web
          que conecte con tu audiencia y genere resultados reales.
        </p>
      </div>

      <!-- Main Content -->
      <div class="contact-content">
        <!-- Profile Card -->
        <article class="info-card profile-card">
            <div class="profile-image-wrapper">
              <div class="image-glow" aria-hidden="true"></div>
              <Image
                src={DeaContact}
                alt="Deanny Bruces - Desarrolladora Web disponible para colaboraciones y proyectos"
                class="profile-image"
                loading="eager"
              />
            </div>

            <div class="profile-content">
              <h2 class="profile-name">Deanny Bruces</h2>
              <p class="profile-role">Desarrolladora Frontend & Estratega Digital</p>

              <p class="profile-text">
                He ayudado a emprendedores y negocios a transformar sus ideas
                en sitios web que no solo lucen increíbles, sino que convierten
                visitantes en clientes.
              </p>

              <p class="profile-text">
                Si buscas una web que refleje tu esencia, aumente tu
                credibilidad y genere resultados medibles, estás en el lugar
                correcto.
              </p>

              <div class="profile-social">
                <SocialIcons />
              </div>
            </div>
          </article>

          <!-- Contact Methods Cards -->
          <div class="contact-methods">
            <article class="method-card">
              <div class="method-icon">
                <svg
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <rect width="20" height="16" x="2" y="4" rx="2"></rect>
                  <path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"></path>
                </svg>
              </div>
              <h3>Email</h3>
              <a href="mailto:deannybruces@gmail.com" class="method-link">
                deannybruces@gmail.com
              </a>
            </article>

            <article class="method-card">
              <div class="method-icon">
                <svg
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <rect width="18" height="18" x="3" y="3" rx="2"></rect>
                  <path d="M7 7h.01M7 12h.01M12 7h.01M12 12h.01M17 7h.01M17 12h.01M7 17h.01M12 17h.01M17 17h.01"></path>
                </svg>
              </div>
              <h3>Consulta estratégica</h3>
              <a
                href="https://calendar.app.google/7zL3cZ713aYB9tCW6"
                target="_blank"
                rel="noopener noreferrer"
                class="method-link"
              >
                Agendar llamada gratis
              </a>
            </article>

            <article class="method-card">
              <div class="method-icon">
                <svg
                  width="24"
                  height="24"
                  viewBox="0 0 24 24"
                  fill="none"
                  stroke="currentColor"
                  stroke-width="2"
                >
                  <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
                  <polyline points="22 4 12 14.01 9 11.01"></polyline>
                </svg>
              </div>
              <h3>Propuesta personalizada</h3>
              <p class="method-text">En 24-48 horas</p>
            </article>
          </div>
        </div>

        <!-- Form Card -->
        <div class="form-card">
          <div class="form-header">
            <h2>Comencemos tu proyecto</h2>
            <p>
              Comparte tu visión y te responderé con ideas concretas sobre cómo
              podemos hacer realidad tu proyecto web. Sin compromiso, solo una
              conversación honesta sobre lo que necesitas.
            </p>
          </div>

          <Formulario />
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* ============================================
     Contact Page Styles - Modern Glassmorphic Design
     ============================================ */

  /* Main Section */
  .contact-section {
    position: relative;
    min-height: 100vh;
    padding: 8rem 0 4rem;
    overflow: hidden;
    background: light-dark(
      linear-gradient(to bottom, #f0f9ff, #faf5ff, #fef3c7),
      linear-gradient(to bottom, #0f172a, #1e1b4b, #312e81)
    );
  }

  /* Background Elements */
  .contact-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    overflow: hidden;
  }

  /* Floating Blobs */
  .floating-blob {
    position: absolute;
    border-radius: 50%;
    opacity: 0.15;
    filter: blur(120px);
    animation: blobFloat 25s ease-in-out infinite;
  }

  .blob-1 {
    top: -10%;
    left: -10%;
    width: 50rem;
    height: 50rem;
    background: linear-gradient(135deg, #a855f7, #ec4899);
    animation-delay: 0s;
  }

  .blob-2 {
    top: 50%;
    right: -15%;
    width: 45rem;
    height: 45rem;
    background: linear-gradient(135deg, #06b6d4, #8b5cf6);
    animation-delay: 8s;
  }

  .blob-3 {
    bottom: -15%;
    left: 30%;
    width: 40rem;
    height: 40rem;
    background: linear-gradient(135deg, #f43f5e, #a855f7);
    animation-delay: 16s;
  }

  /* Gradient Orbs */
  .gradient-orb {
    position: absolute;
    border-radius: 50%;
    opacity: 0.2;
    filter: blur(80px);
    animation: orbPulse 18s ease-in-out infinite;
  }

  .orb-1 {
    top: 25%;
    right: 20%;
    width: 30rem;
    height: 30rem;
    background: radial-gradient(circle, #06b6d4, transparent);
  }

  .orb-2 {
    bottom: 20%;
    left: 15%;
    width: 35rem;
    height: 35rem;
    background: radial-gradient(circle, #a855f7, transparent);
  }

  @keyframes blobFloat {
    0%,
    100% {
      transform: translate(0, 0) scale(1) rotate(0deg);
    }
    33% {
      transform: translate(-35px, -55px) scale(1.15) rotate(120deg);
    }
    66% {
      transform: translate(45px, 35px) scale(0.85) rotate(240deg);
    }
  }

  @keyframes orbPulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 0.2;
    }
    50% {
      transform: scale(1.2);
      opacity: 0.3;
    }
  }

  /* Container */
  .contact-container {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Hero Section */
  .contact-hero {
    text-align: center;
    margin-bottom: 5rem;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.6rem 1.2rem;
    border-radius: 2rem;
    margin-bottom: 1.5rem;

    font-size: 0.9rem;
    font-weight: 600;
    color: light-dark(#8b5cf6, #a78bfa);

    background: light-dark(
      rgba(168, 85, 247, 0.1),
      rgba(168, 85, 247, 0.15)
    );
    border: 1px solid
      light-dark(rgba(168, 85, 247, 0.2), rgba(168, 85, 247, 0.3));

    backdrop-filter: blur(10px);
    animation: badgeGlow 3s ease-in-out infinite;
  }

  .hero-badge svg {
    width: 16px;
    height: 16px;
  }

  @keyframes badgeGlow {
    0%,
    100% {
      box-shadow: 0 0 20px rgba(168, 85, 247, 0.2);
    }
    50% {
      box-shadow: 0 0 30px rgba(168, 85, 247, 0.4);
    }
  }

  .hero-title {
    font-family: "Poppins", sans-serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 1rem;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .gradient-text {
    background: linear-gradient(135deg, #a855f7, #06b6d4, #ec4899);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: gradientShift 8s ease infinite;
  }

  @keyframes gradientShift {
    0%,
    100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.2rem);
    line-height: 1.7;
    color: light-dark(#64748b, #cbd5e1);
    max-width: 700px;
    margin: 0 auto;
  }

  /* Main Content */
  .contact-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: 0.2s;
    opacity: 0;
  }

  /* Info Cards */
  .info-cards {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  /* Profile Card */
  .profile-card {
    position: relative;
    padding: 2.5rem;
    border-radius: 2rem;
    text-align: center;

    background: light-dark(
      rgba(255, 255, 255, 0.8),
      rgba(30, 27, 75, 0.5)
    );
    border: 2px solid
      light-dark(rgba(168, 85, 247, 0.2), rgba(168, 85, 247, 0.4));
    backdrop-filter: blur(20px);

    box-shadow:
      0 10px 40px
        light-dark(rgba(168, 85, 247, 0.1), rgba(0, 0, 0, 0.25)),
      0 0 0 1px
        light-dark(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.05)) inset;

    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .profile-card:hover {
    transform: translateY(-5px);
    border-color: light-dark(
      rgba(168, 85, 247, 0.4),
      rgba(168, 85, 247, 0.6)
    );
  }

  /* Profile Image */
  .profile-image-wrapper {
    position: relative;
    width: 120px;
    height: 120px;
    margin: 0 auto 1.5rem;
  }

  .image-glow {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 110%;
    height: 110%;
    border-radius: 50%;
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    opacity: 0.3;
    filter: blur(25px);
    animation: glowPulse 4s ease-in-out infinite;
  }

  @keyframes glowPulse {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.3;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.1);
      opacity: 0.5;
    }
  }

  .profile-image {
    position: relative;
    width: 120px;
    height: 120px;
    border-radius: 50%;
    object-fit: cover;
    object-position: center;
    border: 3px solid
      light-dark(rgba(168, 85, 247, 0.4), rgba(168, 85, 247, 0.6));
    box-shadow:
      0 10px 30px light-dark(rgba(168, 85, 247, 0.2), rgba(0, 0, 0, 0.4)),
      0 0 0 2px
        light-dark(rgba(255, 255, 255, 0.8), rgba(255, 255, 255, 0.1)) inset;

    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .profile-card:hover .profile-image {
    transform: scale(1.05);
    border-color: light-dark(
      rgba(6, 182, 212, 0.6),
      rgba(6, 182, 212, 0.8)
    );
  }

  /* Profile Content */
  .profile-name {
    margin-bottom: 0.25rem;
    font-family: "Poppins", sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .profile-role {
    margin-bottom: 1.5rem;
    font-size: 1rem;
    font-weight: 600;
    color: light-dark(#8b5cf6, #a78bfa);
  }

  .profile-text {
    margin-bottom: 1.5rem;
    font-size: 0.95rem;
    line-height: 1.7;
    color: light-dark(#64748b, #cbd5e1);
  }

  .profile-social {
    display: flex;
    justify-content: center;
  }

  /* Contact Methods */
  .contact-methods {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  .method-card {
    position: relative;
    padding: 1.5rem;
    border-radius: 1.5rem;

    background: light-dark(
      rgba(255, 255, 255, 0.7),
      rgba(30, 27, 75, 0.4)
    );
    border: 2px solid
      light-dark(rgba(168, 85, 247, 0.15), rgba(168, 85, 247, 0.3));
    backdrop-filter: blur(15px);

    box-shadow:
      0 8px 30px light-dark(rgba(168, 85, 247, 0.08), rgba(0, 0, 0, 0.2)),
      0 0 0 1px
        light-dark(rgba(255, 255, 255, 0.5), rgba(255, 255, 255, 0.05)) inset;

    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .method-card:hover {
    transform: translateY(-3px);
    border-color: light-dark(
      rgba(168, 85, 247, 0.3),
      rgba(168, 85, 247, 0.5)
    );
  }

  .method-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 50px;
    height: 50px;
    margin-bottom: 1rem;
    border-radius: 1rem;

    color: light-dark(#8b5cf6, #a78bfa);
    background: light-dark(
      rgba(168, 85, 247, 0.1),
      rgba(168, 85, 247, 0.15)
    );
    border: 1px solid
      light-dark(rgba(168, 85, 247, 0.2), rgba(168, 85, 247, 0.3));

    transition: all 0.3s ease;
  }

  .method-card:hover .method-icon {
    transform: scale(1.1);
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    border-color: transparent;
    color: #fff;
  }

  .method-card h3 {
    margin-bottom: 0.5rem;
    font-family: "Poppins", sans-serif;
    font-size: 1.1rem;
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .method-link {
    display: inline-block;
    font-size: 0.95rem;
    font-weight: 600;
    color: light-dark(#8b5cf6, #a78bfa);
    text-decoration: none;
    transition: color 0.3s ease;
  }

  .method-link:hover {
    color: light-dark(#7c3aed, #c4b5fd);
  }

  .method-text {
    font-size: 0.95rem;
    color: light-dark(#64748b, #cbd5e1);
  }

  /* Form Card */
  .form-card {
    position: relative;
    padding: 3rem;
    border-radius: 2rem;

    background: light-dark(
      rgba(255, 255, 255, 0.8),
      rgba(30, 27, 75, 0.5)
    );
    border: 2px solid
      light-dark(rgba(168, 85, 247, 0.2), rgba(168, 85, 247, 0.4));
    backdrop-filter: blur(20px);

    box-shadow:
      0 10px 40px
        light-dark(rgba(168, 85, 247, 0.1), rgba(0, 0, 0, 0.25)),
      0 0 0 1px
        light-dark(rgba(255, 255, 255, 0.6), rgba(255, 255, 255, 0.05)) inset;

    transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .form-header {
    margin-bottom: 2rem;
  }

  .form-header h2 {
    margin-bottom: 0.5rem;
    font-family: "Poppins", sans-serif;
    font-size: 1.75rem;
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .form-header p {
    font-size: 1rem;
    line-height: 1.6;
    color: light-dark(#64748b, #cbd5e1);
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }

  /* Responsive Adjustments */
  @media (min-width: 768px) {
    .contact-methods {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .contact-content {
      grid-template-columns: 400px 1fr;
      gap: 3rem;
    }

    .profile-card {
      padding: 3rem 2.5rem;
    }
  }

  @media (max-width: 767px) {
    .contact-section {
      padding: 6rem 0 3rem;
    }

    .contact-hero {
      margin-bottom: 3rem;
    }

    .form-card {
      padding: 2rem 1.5rem;
    }

    .profile-card {
      padding: 2rem 1.5rem;
    }

    .profile-image-wrapper {
      width: 100px;
      height: 100px;
    }

    .profile-image {
      width: 100px;
      height: 100px;
    }

    .contact-methods {
      gap: 1rem;
    }
  }

  @media (min-width: 1400px) {
    .contact-container {
      padding: 0 2rem;
    }
  }
</style>
