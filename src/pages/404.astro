---
import Layout from "../layouts/Layout.astro";
---

<Layout
  title="404 - Página no encontrada"
  description="La página que buscas no existe. Regresa al inicio del portfolio de Deanny Bruces para explorar proyectos y contenido sobre desarrollo web."
  keywords="404, página no encontrada, error, Deanny Bruces"
>
  <style>
    .not-found {
      position: relative;

      overflow: hidden;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;

      width: 100vw;
      min-height: 100vh;
      margin: 0;
      padding: 0;
      border-radius: 0;

      color: var(--h1-h2-color, #6e6b6b);

      background: var(--bg-color, #f2e9f2);
      box-shadow: none;

      animation: fade-in-move 1.2s cubic-bezier(0.77, 0, 0.175, 1) both;
    }

    .not-found-content {
      z-index: 1;

      width: 90vw;
      max-width: 500px;
      padding: 3rem 2rem;
      border-radius: var(--border-radius, 20px);

      text-align: center;

      background: rgb(255 255 255 / 85%);
      box-shadow: 0 4px 24px rgb(80 12 75 / 12%);
    }

    .not-found h1 {
      cursor: pointer;

      margin-bottom: 0.5rem;

      font-size: 6rem;
      color: var(--secondary-color, #c38cf6);
      text-shadow: 0 2px 16px var(--secondary-color, #c38cf6);
      letter-spacing: 0.1em;

      transition: transform 0.3s;
      animation: bounce404 1.5s infinite alternate;
    }

    .not-found h1:hover {
      transform: scale(1.15) rotate(-5deg);
      filter: brightness(1.2);
    }

    .not-found h2 {
      margin-bottom: 1rem;
      font-size: 2rem;
      color: var(--primary-color, #2d0c4b);
      animation: fade-in-text 1.5s 0.5s both;
    }

    .not-found p {
      margin-bottom: 2rem;
      color: var(--secondary-color, #817d7d);
      animation: fade-in-text 1.5s 0.8s both;
    }

    .not-found a {
      display: inline-block;

      padding: 0.75rem 2rem;
      border-radius: 8px;

      font-weight: bold;
      color: #fff;
      text-decoration: none;

      background: var(--acento-btn, #dda0dd);
      box-shadow: 0 2px 8px rgb(195 140 246 / 15%);

      transition:
        background 0.3s,
        transform 0.2s;
      animation: fade-in-text 1.5s 1.1s both;
    }

    .not-found a:hover {
      transform: scale(1.08);
      background: var(--acento-btn-hover, #c38cf6);
    }

    /* Decorative SVGs */
    .svg-deco {
      pointer-events: none;

      position: absolute;
      z-index: 0;

      opacity: 0.18;

      animation: float-deco 6s ease-in-out infinite alternate;
    }

    .svg-deco.top {
      top: -40px;
      left: -40px;

      width: 120px;
      height: 120px;

      fill: var(--secondary-color, #c38cf6);
    }

    .svg-deco.bottom {
      right: -40px;
      bottom: -40px;

      width: 100px;
      height: 100px;

      fill: var(--primary-color, #2d0c4b);

      animation-delay: 2s;
    }

    /* Responsive */
    @media (width <= 600px) {
      .not-found-content {
        max-width: 98vw;
        padding: 2rem 0.5rem;
      }

      .not-found h1 {
        font-size: 3.5rem;
      }

      .not-found h2 {
        font-size: 1.2rem;
      }
    }

    @keyframes fade-in-move {
      0% {
        transform: translateY(40px) scale(0.95);
        opacity: 0;
      }

      100% {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
    }

    @keyframes bounce404 {
      0% {
        transform: scale(1) translateY(0);
      }

      100% {
        transform: scale(1.08) translateY(-10px);
      }
    }

    @keyframes fade-in-text {
      0% {
        transform: translateY(20px);
        opacity: 0;
      }

      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes float-deco {
      0% {
        transform: translateY(0) scale(1);
      }

      100% {
        transform: translateY(20px) scale(1.08);
      }
    }
  </style>

  <section class="not-found">
    <svg class="svg-deco top" viewBox="0 0 100 100"
      ><circle cx="50" cy="50" r="50"></circle></svg
    >
    <svg class="svg-deco bottom" viewBox="0 0 100 100"
      ><rect x="10" y="10" width="80" height="80" rx="20"></rect></svg
    >
    <div class="not-found-content">
      <h1 tabindex="0">404</h1>
      <h2>Página no encontrada</h2>
      <p>Lo sentimos, la página que buscas no existe o fue movida.</p>
      <a href="/">Volver al inicio</a>
    </div>
  </section>
</Layout>
