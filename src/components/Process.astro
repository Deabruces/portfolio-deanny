<section class="process-section content-grid">
  <div class="process-container">
    <!-- Header -->
    <div class="process-header">
      <div class="header-badge">
        <svg
          class="badge-icon"
          width="16"
          height="16"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path d="M12 5v14M5 12l14 0"></path>
        </svg>
        <span>Nuestro Proceso</span>
      </div>
      <h2 class="process-title">
        Así convertimos ideas
        <br />
        en sitios web increíbles
      </h2>
      <p class="process-subtitle">
        Un proceso probado que garantiza resultados extraordinarios
      </p>
    </div>

    <!-- Grid de pasos -->
    <div class="steps-grid">
      <!-- Paso 1: Descubrimiento -->
      <div class="step-card step-purple">
        <div class="step-badge">Paso 1</div>

        <div class="step-icon-wrapper icon-purple">
          <svg
            class="step-icon"
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <path d="M15 12a3 3 0 1 1-6 0 3 3 0 0 1 6 0Z"></path>
            <path
              d="M12 4v1M18 6l-1 1M20 12h-1M18 18l-1-1M12 19v1M7 17l-1 1M5 12H4M7 7 6 6"
            ></path>
          </svg>
        </div>

        <div class="step-content">
          <h3 class="step-title">Descubrimiento</h3>
          <p class="step-subtitle">Entendemos tu visión</p>
          <p class="step-description">
            Nos sumergimos en tu negocio, objetivos y audiencia. Investigamos a
            tu competencia y definimos la estrategia perfecta para destacar.
          </p>

          <ul class="step-details">
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Briefing detallado
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Investigación de mercado
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Definición de objetivos
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Análisis de competencia
            </li>
          </ul>
        </div>

        <div class="step-number">1</div>
      </div>

      <!-- Paso 2: Diseño -->
      <div class="step-card step-blue">
        <div class="step-badge">Paso 2</div>

        <div class="step-icon-wrapper icon-blue">
          <svg
            class="step-icon"
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <path d="M17 3a2.8 2.8 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z"></path>
          </svg>
        </div>

        <div class="step-content">
          <h3 class="step-title">Diseño</h3>
          <p class="step-subtitle">Creamos la experiencia visual</p>
          <p class="step-description">
            Diseñamos wireframes, moodboards y prototipos interactivos. Tu marca
            cobra vida con interfaces que enamoran.
          </p>

          <ul class="step-details">
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Wireframes
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Moodboards
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Prototipos interactivos
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Sistema de diseño
            </li>
          </ul>
        </div>

        <div class="step-number">2</div>
      </div>

      <!-- Paso 3: Desarrollo -->
      <div class="step-card step-cyan">
        <div class="step-badge">Paso 3</div>

        <div class="step-icon-wrapper icon-cyan">
          <svg
            class="step-icon"
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="16 18 22 12 16 6"></polyline>
            <polyline points="8 6 2 12 8 18"></polyline>
          </svg>
        </div>

        <div class="step-content">
          <h3 class="step-title">Desarrollo</h3>
          <p class="step-subtitle">Construimos tu sitio web</p>
          <p class="step-description">
            Convertimos el diseño en código limpio, rápido y optimizado.
            Responsive, accesible y con las mejores prácticas.
          </p>

          <ul class="step-details">
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Código limpio y semántico
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Responsive design
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Optimización SEO
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Performance superior
            </li>
          </ul>
        </div>

        <div class="step-number">3</div>
      </div>

      <!-- Paso 4: Lanzamiento -->
      <div class="step-card step-green">
        <div class="step-badge">Paso 4</div>

        <div class="step-icon-wrapper icon-green">
          <svg
            class="step-icon"
            width="32"
            height="32"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <path
              d="M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z"
            ></path>
            <path
              d="m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z"
            ></path>
            <path d="M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0"></path>
            <path d="M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5"></path>
          </svg>
        </div>

        <div class="step-content">
          <h3 class="step-title">Lanzamiento</h3>
          <p class="step-subtitle">Despegamos juntos</p>
          <p class="step-description">
            Probamos todo al detalle, configuramos analytics y lanzamos tu sitio
            al mundo. Tu éxito es nuestro éxito.
          </p>

          <ul class="step-details">
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Testing exhaustivo
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Configuración de analytics
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Deploy y DNS
            </li>
            <li class="detail-item">
              <span class="detail-bullet">→</span>
              Soporte post-lanzamiento
            </li>
          </ul>
        </div>

        <div class="step-number">4</div>
      </div>
    </div>

    <!-- CTA final -->
    <div class="process-cta">
      <div class="cta-content">
        <h3 class="cta-title">¿Listo para comenzar?</h3>
        <p class="cta-text">
          Trabajemos juntos en tu próximo proyecto increíble
        </p>
        <a href="#contacto" class="cta-button">
          Iniciar mi proyecto
          <span class="button-arrow">→</span>
        </a>
      </div>

      <div class="floating-badge">✨ De la idea a la web</div>
    </div>
  </div>
</section>

<style>
  /* Variables */
  :root {
    --purple-light: #a855f7;
    --purple-dark: #7c3aed;
    --blue-light: #3b82f6;
    --blue-dark: #2563eb;
    --cyan-light: #06b6d4;
    --cyan-dark: #0891b2;
    --green-light: #22c55e;
    --green-dark: #16a34a;
  }

  /* Sección principal */
  .process-section {
    position: relative;

    overflow: hidden;

    min-height: 100vh;
    padding: 6rem 1.5rem;

    background: light-dark(
      linear-gradient(to bottom, #f8fafc, #f1f5f9),
      linear-gradient(to bottom, #0f172a, #1e1b4b)
    );

    transition: background 0.3s ease;
  }

  .process-container {
    max-width: 1280px;
    margin: 0 auto;
  }

  /* Header */
  .process-header {
    position: relative;
    margin-bottom: 4rem;
    text-align: center;
  }

  .header-badge {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;

    margin-bottom: 1.5rem;
    padding: 0.5rem 1.5rem;
    border: 1px solid light-dark(rgb(124 58 237 / 20%), rgb(168 85 247 / 30%));
    border-radius: 9999px;

    font-size: 0.875rem;
    font-weight: 600;
    color: light-dark(#7c3aed, #e9d5ff);

    background: light-dark(rgb(124 58 237 / 10%), rgb(168 85 247 / 20%));

    animation: fadeInDown 0.6s ease-out;
  }

  .badge-icon {
    width: 1rem;
    height: 1rem;
  }

  .process-title {
    margin-bottom: 1rem;

    font-size: 3rem;
    font-weight: bold;
    line-height: 1.2;
    color: light-dark(#0f172a, #fff);

    animation: fadeInUp 0.6s ease-out 0.1s both;
  }

  .process-subtitle {
    max-width: 600px;
    margin: 0 auto;

    font-size: 1.25rem;
    color: light-dark(#64748b, #94a3b8);

    animation: fadeInUp 0.6s ease-out 0.2s both;
  }

  /* Grid de pasos */
  .steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    margin-bottom: 4rem;
  }

  /* Tarjeta de paso */
  .step-card {
    cursor: pointer;

    position: relative;

    overflow: hidden;

    padding: 2.5rem;
    border: 1px solid light-dark(rgb(0 0 0 / 5%), rgb(255 255 255 / 10%));
    border-radius: 1.5rem;

    background: light-dark(#fff, rgb(30 27 75 / 60%));
    box-shadow: light-dark(0 4px 6px -1px rgb(0 0 0 / 5%), none);

    transition: all 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .step-card:hover {
    transform: translateY(-4px);
    border-color: light-dark(rgb(0 0 0 / 10%), rgb(255 255 255 / 20%));
    background: light-dark(#fff, rgb(30 27 75 / 80%));
    box-shadow: light-dark(
      0 20px 25px -5px rgb(0 0 0 / 10%),
      0 20px 25px -5px rgb(0 0 0 / 30%)
    );
  }

  /* Badge del paso */
  .step-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    transform: rotate(8deg);

    padding: 0.375rem 0.875rem;
    border-radius: 9999px;

    font-size: 0.75rem;
    font-weight: bold;
    color: white;
  }

  .step-card.step-purple .step-badge {
    background: light-dark(var(--purple-dark), var(--purple-light));
  }

  .step-card.step-blue .step-badge {
    background: light-dark(var(--blue-dark), var(--blue-light));
  }

  .step-card.step-cyan .step-badge {
    background: light-dark(var(--cyan-dark), var(--cyan-light));
  }

  .step-card.step-green .step-badge {
    background: light-dark(var(--green-dark), var(--green-light));
  }

  /* Icono del paso */
  .step-icon-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;

    width: 4rem;
    height: 4rem;
    margin-bottom: 1.5rem;
    border-radius: 1rem;

    transition: transform 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .step-card:hover .step-icon-wrapper {
    transform: scale(1.08) rotate(3deg);
  }

  .icon-purple {
    background: linear-gradient(
      135deg,
      var(--purple-light),
      var(--purple-dark)
    );
  }

  .icon-blue {
    background: linear-gradient(135deg, var(--blue-light), var(--blue-dark));
  }

  .icon-cyan {
    background: linear-gradient(135deg, var(--cyan-light), var(--cyan-dark));
  }

  .icon-green {
    background: linear-gradient(135deg, var(--green-light), var(--green-dark));
  }

  .step-icon {
    width: 2rem;
    height: 2rem;
    color: white;
  }

  /* Contenido del paso */
  .step-content {
    position: relative;
    z-index: 2;
  }

  .step-title {
    margin-bottom: 0.5rem;
    font-size: 1.875rem;
    font-weight: bold;
    color: light-dark(#0f172a, #fff);
  }

  .step-subtitle {
    margin-bottom: 1rem;
    font-size: 1rem;
    font-weight: 600;
    color: light-dark(#7c3aed, #a78bfa);
  }

  .step-description {
    margin-bottom: 1.5rem;
    font-size: 1rem;
    line-height: 1.6;
    color: light-dark(#475569, #cbd5e1);
  }

  /* Detalles (aparecen en hover) */
  .step-details {
    overflow: hidden;

    max-height: 0;
    margin: 0;
    padding: 0;

    list-style: none;

    opacity: 0;

    transition:
      max-height 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94),
      opacity 0.5s ease 0.1s;
  }

  .step-card:hover .step-details {
    max-height: 300px;
    margin-top: 1rem;
    opacity: 1;
  }

  .detail-item {
    display: flex;
    gap: 0.5rem;
    align-items: center;

    padding: 0.5rem 0;

    font-size: 0.875rem;
    color: light-dark(#64748b, #94a3b8);
  }

  .detail-bullet {
    font-size: 1rem;
    font-weight: bold;
    color: light-dark(#7c3aed, #a78bfa);
  }

  /* Número decorativo */
  .step-number {
    pointer-events: none;

    position: absolute;
    right: -1rem;
    bottom: -2rem;

    font-size: 10rem;
    font-weight: bold;
    line-height: 1;
    color: light-dark(#0f172a, #fff);

    opacity: 0.05;

    transition: all 0.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .step-card:hover .step-number {
    transform: scale(1.05);
    opacity: 0.08;
  }

  /* CTA final */
  .process-cta {
    position: relative;

    overflow: hidden;

    padding: 4rem 2rem;
    border: 1px solid light-dark(rgb(124 58 237 / 10%), rgb(168 85 247 / 30%));
    border-radius: 2rem;

    text-align: center;

    background: light-dark(
      linear-gradient(135deg, rgb(124 58 237 / 5%), rgb(168 85 247 / 5%)),
      linear-gradient(135deg, rgb(124 58 237 / 20%), rgb(168 85 247 / 20%))
    );
  }

  .cta-content {
    position: relative;
    z-index: 2;
  }

  .cta-title {
    margin-bottom: 1rem;
    font-size: 2.5rem;
    font-weight: bold;
    color: light-dark(#0f172a, #fff);
  }

  .cta-text {
    margin-bottom: 2rem;
    font-size: 1.125rem;
    color: light-dark(#64748b, #cbd5e1);
  }

  .cta-button {
    cursor: pointer;

    display: inline-flex;
    gap: 0.5rem;
    align-items: center;

    padding: 1rem 2.5rem;
    border: none;
    border-radius: 9999px;

    font-size: 1.125rem;
    font-weight: bold;
    color: white;
    text-decoration: none;

    background: linear-gradient(
      135deg,
      var(--purple-dark),
      var(--purple-light)
    );
    box-shadow: 0 10px 15px -3px rgb(0 0 0 / 20%);

    transition: all 0.3s ease;
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 20px 25px -5px rgb(124 58 237 / 40%);
  }

  .button-arrow {
    transition: transform 0.3s ease;
  }

  .cta-button:hover .button-arrow {
    transform: translateX(4px);
  }

  /* Badge flotante */
  .floating-badge {
    position: absolute;
    top: 6rem;
    right: 2rem;
    transform: rotate(-8deg);

    padding: 0.5rem 1.5rem;
    border-radius: 9999px;

    font-size: 0.875rem;
    font-weight: bold;
    color: light-dark(white, #000);

    background: light-dark(#7c3aed, #fbbf24);

    animation: float 3s ease-in-out infinite;
  }

  @media (width >= 768px) {
    .process-title {
      font-size: 4rem;
    }
  }

  @media (width >= 768px) {
    .steps-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (width >= 1024px) {
    .steps-grid {
      gap: 2.5rem;
    }
  }

  /* Responsive adjustments */
  @media (width <= 767px) {
    .process-section {
      padding: 4rem 1rem;
    }

    .process-title {
      font-size: 2rem;
    }

    .cta-title {
      font-size: 1.875rem;
    }

    .step-card {
      padding: 2rem;
    }

    .floating-badge {
      display: none;
    }
  }

  /* Animaciones */
  @keyframes fadeInDown {
    from {
      transform: translateY(-20px);
      opacity: 0;
    }

    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes fadeInUp {
    from {
      transform: translateY(20px);
      opacity: 0;
    }

    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0) rotate(-8deg);
    }

    50% {
      transform: translateY(-10px) rotate(-8deg);
    }
  }
</style>

<script>
  // Añadir animación cuando los elementos entran en vista
  document.addEventListener("DOMContentLoaded", () => {
    const cards = document.querySelectorAll(".step-card");

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry, index) => {
          if (entry.isIntersecting) {
            setTimeout(() => {
              entry.target.style.animation = `fadeInUp 0.6s ease-out ${index * 0.1}s both`;
            }, index * 100);
            observer.unobserve(entry.target);
          }
        });
      },
      { threshold: 0.1 },
    );

    cards.forEach((card) => {
      card.style.opacity = "0";
      observer.observe(card);
    });
  });
</script>
