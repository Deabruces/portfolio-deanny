---
// FAQ Home component - Top questions before taking action
---

<section class="faq-home content-grid">
  <div class="faq-container">
    <!-- Header -->
    <div class="faq-header">
      <h2 class="faq-title">
        Preguntas frecuentes
        <span class="gradient-text">antes de empezar</span>
      </h2>
      <p class="faq-subtitle">
        Resuelve tus dudas más comunes antes de dar el siguiente paso
      </p>
    </div>

    <!-- FAQ Items -->
    <div class="faq-list">
      <!-- FAQ 1 -->
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span>¿Cuánto cuesta desarrollar mi sitio web?</span>
          <svg
            class="faq-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-answer">
          <p>
            Los proyectos comienzan desde <strong>$800 USD</strong> para sitios
            básicos. Proyectos más complejos con e-commerce, automatización o
            IA varían según tus necesidades. Siempre recibirás un presupuesto
            detallado antes de comenzar, con opciones de pago flexibles.
          </p>
        </div>
      </div>

      <!-- FAQ 2 -->
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span>¿Cuánto tiempo toma tener mi web lista?</span>
          <svg
            class="faq-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-answer">
          <p>
            Un sitio web básico está listo en <strong>2-3 semanas</strong>.
            Proyectos más complejos como e-commerce toman entre 6-12 semanas.
            Durante nuestra primera llamada te daré un cronograma exacto según
            tu proyecto específico.
          </p>
        </div>
      </div>

      <!-- FAQ 3 -->
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span>¿Qué incluye el servicio?</span>
          <svg
            class="faq-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-answer">
          <p>
            Diseño profesional responsive, desarrollo optimizado, SEO básico,
            integración con herramientas (formularios, analytics), capacitación
            para que puedas gestionar tu contenido, y <strong
              >30 días de soporte gratuito</strong
            > post-lanzamiento.
          </p>
        </div>
      </div>

      <!-- FAQ 4 -->
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span>¿Necesito tener todo el contenido listo?</span>
          <svg
            class="faq-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-answer">
          <p>
            No es necesario. Puedo ayudarte con la creación de contenido,
            redacción optimizada para SEO y selección de imágenes profesionales.
            Si ya tienes materiales, perfecto. Si no, te guío en cada paso.
          </p>
        </div>
      </div>

      <!-- FAQ 5 -->
      <div class="faq-item">
        <button class="faq-question" aria-expanded="false">
          <span>¿Trabajas con clientes internacionales?</span>
          <svg
            class="faq-icon"
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="6 9 12 15 18 9"></polyline>
          </svg>
        </button>
        <div class="faq-answer">
          <p>
            ¡Por supuesto! Trabajo con clientes en toda <strong
              >América Latina, España y Estados Unidos</strong
            >. Todo el proceso es 100% remoto. Acepto pagos internacionales y
            me adapto a diferentes zonas horarias.
          </p>
        </div>
      </div>
    </div>

    <!-- More Questions CTA -->
    <div class="more-questions">
      <p>¿Tienes más preguntas?</p>
      <a href="/servicios#faq" class="more-link">
        Ver todas las preguntas frecuentes
        <svg
          width="18"
          height="18"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <line x1="5" y1="12" x2="19" y2="12"></line>
          <polyline points="12 5 19 12 12 19"></polyline>
        </svg>
      </a>
    </div>
  </div>
</section>

<script>
  document.addEventListener("DOMContentLoaded", () => {
    const faqItems = document.querySelectorAll(".faq-home .faq-item");

    faqItems.forEach((item) => {
      const question = item.querySelector(".faq-question");

      question?.addEventListener("click", () => {
        const isActive = item.classList.contains("active");

        // Close all other items
        faqItems.forEach((otherItem) => {
          if (otherItem !== item) {
            otherItem.classList.remove("active");
            const otherQuestion = otherItem.querySelector(".faq-question");
            if (otherQuestion) {
              otherQuestion.setAttribute("aria-expanded", "false");
            }
          }
        });

        // Toggle current item
        item.classList.toggle("active");
        question.setAttribute("aria-expanded", (!isActive).toString());
      });

      // Keyboard support
      question?.addEventListener("keydown", (e) => {
        if (e.key === "Enter" || e.key === " ") {
          e.preventDefault();
          question.click();
        }
      });
    });
  });
</script>

<style>
  /* Main Section */
  .faq-home {
    padding: 6rem 0;
    background: light-dark(
      linear-gradient(to bottom, #ffffff, #fafafa),
      linear-gradient(to bottom, #0f172a, #1a1a2e)
    );
  }

  .faq-container {
    width: 100%;
    max-width: 800px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Header */
  .faq-header {
    margin-bottom: 3rem;
    text-align: center;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .faq-title {
    margin-bottom: 1rem;
    font-family: Poppins, sans-serif;
    font-size: clamp(2rem, 5vw, 2.5rem);
    font-weight: 800;
    line-height: 1.2;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .gradient-text {
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .faq-subtitle {
    font-size: 1.05rem;
    color: light-dark(#64748b, #cbd5e1);
  }

  /* FAQ List */
  .faq-list {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-bottom: 3rem;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.1s forwards;
  }

  /* FAQ Item */
  .faq-item {
    overflow: hidden;
    border: 2px solid light-dark(rgb(168 85 247 / 15%), rgb(168 85 247 / 25%));
    border-radius: 1rem;
    background: light-dark(rgb(255 255 255 / 90%), rgb(30 27 75 / 50%));
    backdrop-filter: blur(10px);
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .faq-item:hover {
    border-color: light-dark(rgb(168 85 247 / 30%), rgb(168 85 247 / 40%));
    box-shadow: 0 4px 20px light-dark(rgb(168 85 247 / 8%), rgb(0 0 0 / 20%));
  }

  .faq-question {
    cursor: pointer;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    width: 100%;
    padding: 1.25rem 1.75rem;
    border: none;
    background: transparent;
    font-family: Poppins, sans-serif;
    font-size: 1.05rem;
    font-weight: 600;
    color: light-dark(#1e1b4b, #f8fafc);
    text-align: left;
    transition: color 0.3s ease;
  }

  .faq-question:hover {
    color: light-dark(#8b5cf6, #a78bfa);
  }

  .faq-question:focus-visible {
    outline: 3px solid light-dark(#a855f7, #a78bfa);
    outline-offset: -3px;
  }

  .faq-icon {
    flex-shrink: 0;
    width: 24px;
    height: 24px;
    color: light-dark(#8b5cf6, #a78bfa);
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .faq-item.active .faq-icon {
    transform: rotate(180deg);
  }

  .faq-answer {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .faq-item.active .faq-answer {
    max-height: 300px;
  }

  .faq-answer p {
    padding: 0 1.75rem 1.25rem;
    font-size: 1rem;
    line-height: 1.7;
    color: light-dark(#475569, #cbd5e1);
  }

  .faq-answer strong {
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  /* More Questions */
  .more-questions {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    align-items: center;
    padding: 2rem;
    border: 2px solid light-dark(rgb(168 85 247 / 15%), rgb(168 85 247 / 25%));
    border-radius: 1rem;
    text-align: center;
    background: light-dark(rgb(250 245 255 / 60%), rgb(30 27 75 / 30%));
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
  }

  .more-questions p {
    margin: 0;
    font-size: 1.05rem;
    font-weight: 600;
    color: light-dark(#475569, #cbd5e1);
  }

  .more-link {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    font-size: 1rem;
    font-weight: 700;
    color: light-dark(#8b5cf6, #a78bfa);
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .more-link:hover {
    gap: 0.75rem;
    color: light-dark(#7c3aed, #c4b5fd);
  }

  .more-link svg {
    width: 18px;
    height: 18px;
    transition: transform 0.3s ease;
  }

  .more-link:hover svg {
    transform: translateX(4px);
  }

  /* Responsive */
  @media (width <= 767px) {
    .faq-home {
      padding: 4rem 0;
    }

    .faq-header {
      margin-bottom: 2rem;
    }

    .faq-question {
      padding: 1rem 1.25rem;
      font-size: 0.95rem;
    }

    .faq-answer p {
      padding: 0 1.25rem 1rem;
      font-size: 0.95rem;
    }

    .more-questions {
      padding: 1.5rem;
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      transform: translateY(30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }
</style>
