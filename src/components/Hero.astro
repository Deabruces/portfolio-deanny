---
import { Button } from "deanny-uikit";
---

<section class="hero full-width">
  <h2 class="saludo">
    Hi, <span class="greeting"></span>, I'm Deanny! 👋🏻
  </h2>
  <h1 class="title">Frontend Developer</h1>
  <p class="frase">
    "I code efficient, accessible, and delightful web experiences."
  </p>
    <Button label="Connect With me" type="primary" />
</section>
<style>
  .hero {
    height: 100dvh;
    display: grid;
    justify-items: center;
    align-items: center;
    align-content: center;
    isolation: isolate;
  }

  .title {
    font-weight: 700;
    text-align: center;
  }
  .saludo {
    font-weight: 600;
    text-align: center;
  }
  .frase {
    font-style: italic;
    text-align: center;
    max-width: 600px;
  }

  .btn__contact {
    display: block;
    padding: 15px 30px;
    font-size: 1.5rem;
    font-weight: 600;
    background-color: var(--background-color);
    border: 1px solid var(--acento-btn);
    border-radius: var(--border-radius);
    cursor: pointer;
    transition:
      background-color 0.3s ease,
      transform 0.3s ease;
    margin-top: 20px;
  }

  .btn__contact:hover {
    background-color: var(--background-color-hover);
    transform: scale(1.05);
  }

  .btn__contact-about {
    font-size: 2rem;
  }

  .greeting {
    font-weight: 600;
    color: var(--acento-btn-hover);
    border-radius: 20px;
    border: 1px solid var(--acento-btn-hover);
    padding: 0.2rem 1rem;
  }
</style>
