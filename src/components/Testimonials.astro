---
// Testimonials & Social Proof component
---

<section class="testimonials-section content-grid">
  <!-- Background Elements -->
  <div class="testimonials-background full-width" aria-hidden="true">
    <div class="floating-blob blob-1"></div>
    <div class="floating-blob blob-2"></div>
    <div class="gradient-orb"></div>
  </div>

  <div class="testimonials-container">
    <!-- Header -->
    <div class="testimonials-header">
      <div class="header-badge">
        <svg
          class="badge-icon"
          width="16"
          height="16"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <polygon
            points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"
          ></polygon>
        </svg>
        <span>Testimonios</span>
      </div>
      <h2 class="testimonials-title">
        Lo que dicen quienes ya
        <span class="gradient-text">trabajan conmigo</span>
      </h2>
      <p class="testimonials-subtitle">
        Resultados reales de emprendedores que transformaron su presencia digital
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div class="testimonials-grid">
      <!-- Testimonial 1 -->
      <article class="testimonial-card featured">
        <div class="card-header">
          <div class="avatar">
            <svg
              width="48"
              height="48"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
              <circle cx="12" cy="7" r="4"></circle>
            </svg>
          </div>
          <div class="client-info">
            <h3 class="client-name">María González</h3>
            <p class="client-business">Fundadora · Studio de Yoga Online</p>
          </div>
          <div class="rating" aria-label="Calificación: 5 de 5 estrellas">
            ⭐⭐⭐⭐⭐
          </div>
        </div>
        <blockquote class="testimonial-text">
          "Pasé de tener una web que nadie visitaba a generar +60% más reservas
          en solo 2 meses. El diseño es hermoso y mis clientes ahora me
          encuentran en Google sin pagar anuncios."
        </blockquote>
        <div class="result-badge">
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
          </svg>
          <span>+60% más reservas en 2 meses</span>
        </div>
      </article>

      <!-- Testimonial 2 -->
      <article class="testimonial-card">
        <div class="card-header">
          <div class="avatar">
            <svg
              width="48"
              height="48"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
              <circle cx="12" cy="7" r="4"></circle>
            </svg>
          </div>
          <div class="client-info">
            <h3 class="client-name">Carlos Ramírez</h3>
            <p class="client-business">CEO · Consultoría Empresarial</p>
          </div>
          <div class="rating" aria-label="Calificación: 5 de 5 estrellas">
            ⭐⭐⭐⭐⭐
          </div>
        </div>
        <blockquote class="testimonial-text">
          "Mi antigua web se veía amateur. Ahora transmite la seriedad que
          necesitaba para cerrar contratos grandes. Las consultas de calidad
          aumentaron un 45%."
        </blockquote>
        <div class="result-badge">
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
          </svg>
          <span>+45% consultas de calidad</span>
        </div>
      </article>

      <!-- Testimonial 3 -->
      <article class="testimonial-card">
        <div class="card-header">
          <div class="avatar">
            <svg
              width="48"
              height="48"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
            >
              <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
              <circle cx="12" cy="7" r="4"></circle>
            </svg>
          </div>
          <div class="client-info">
            <h3 class="client-name">Ana Sofía Martínez</h3>
            <p class="client-business">Dueña · Tienda Online de Joyas</p>
          </div>
          <div class="rating" aria-label="Calificación: 5 de 5 estrellas">
            ⭐⭐⭐⭐⭐
          </div>
        </div>
        <blockquote class="testimonial-text">
          "Invertí en un e-commerce profesional y fue la mejor decisión. Mis
          ventas se triplicaron y ahora aparezco en la primera página de Google.
          ¡Increíble!"
        </blockquote>
        <div class="result-badge">
          <svg
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <polyline points="22 12 18 12 15 21 9 3 6 12 2 12"></polyline>
          </svg>
          <span>3x ventas online</span>
        </div>
      </article>
    </div>

    <!-- Stats -->
    <div class="stats-row">
      <div class="stat-item">
        <div class="stat-number">100%</div>
        <div class="stat-label">Tasa de satisfacción</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">+50</div>
        <div class="stat-label">Clientes felices</div>
      </div>
      <div class="stat-item">
        <div class="stat-number">4.9/5</div>
        <div class="stat-label">Calificación promedio</div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Main Section */
  .testimonials-section {
    position: relative;
    overflow: hidden;
    padding: 8rem 0;
    background: light-dark(
      linear-gradient(to bottom, #faf5ff, #eff6ff),
      linear-gradient(to bottom, #1e1b4b, #0f172a)
    );
  }

  /* Background Elements */
  .testimonials-background {
    pointer-events: none;
    position: absolute;
    z-index: 0;
    top: 0;
    left: 0;
    overflow: hidden;
    width: 100%;
    height: 100%;
  }

  .floating-blob {
    position: absolute;
    width: 35rem;
    height: 35rem;
    border-radius: 50%;
    opacity: 0.1;
    filter: blur(100px);
    animation: blobFloat 20s ease-in-out infinite;
  }

  .blob-1 {
    top: -10%;
    left: -10%;
    background: linear-gradient(135deg, #a855f7, #ec4899);
  }

  .blob-2 {
    right: -10%;
    bottom: -10%;
    background: linear-gradient(135deg, #06b6d4, #8b5cf6);
    animation-delay: 10s;
  }

  .gradient-orb {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 25rem;
    height: 25rem;
    border-radius: 50%;
    opacity: 0.12;
    background: radial-gradient(circle, #f59e0b, transparent);
    filter: blur(80px);
    animation: orbPulse 15s ease-in-out infinite;
  }

  /* Container */
  .testimonials-container {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Header */
  .testimonials-header {
    margin-bottom: 4rem;
    text-align: center;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .header-badge {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    margin-bottom: 1.5rem;
    padding: 0.6rem 1.2rem;
    border: 1px solid light-dark(rgb(245 158 11 / 30%), rgb(245 158 11 / 40%));
    border-radius: 2rem;
    font-size: 0.9rem;
    font-weight: 600;
    color: light-dark(#f59e0b, #fbbf24);
    background: light-dark(rgb(245 158 11 / 10%), rgb(245 158 11 / 15%));
  }

  .badge-icon {
    width: 1rem;
    height: 1rem;
    fill: currentColor;
    stroke: none;
  }

  .testimonials-title {
    margin-bottom: 1rem;
    font-family: Poppins, sans-serif;
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    line-height: 1.2;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .gradient-text {
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .testimonials-subtitle {
    font-size: 1.125rem;
    color: light-dark(#64748b, #cbd5e1);
  }

  /* Testimonials Grid */
  .testimonials-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    margin-bottom: 4rem;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
  }

  /* Testimonial Card */
  .testimonial-card {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 2.5rem;
    border: 2px solid light-dark(rgb(168 85 247 / 15%), rgb(168 85 247 / 25%));
    border-radius: 1.5rem;
    background: light-dark(rgb(255 255 255 / 90%), rgb(30 27 75 / 50%));
    backdrop-filter: blur(10px);
    box-shadow:
      0 8px 30px light-dark(rgb(168 85 247 / 10%), rgb(0 0 0 / 20%)),
      0 0 0 1px light-dark(rgb(255 255 255 / 60%), rgb(255 255 255 / 5%)) inset;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .testimonial-card.featured {
    border-color: light-dark(rgb(245 158 11 / 30%), rgb(245 158 11 / 40%));
    background: light-dark(
      linear-gradient(135deg, rgb(254 252 232 / 90%), rgb(255 255 255 / 90%)),
      linear-gradient(135deg, rgb(120 53 15 / 15%), rgb(30 27 75 / 50%))
    );
  }

  .testimonial-card:hover {
    transform: translateY(-8px);
    border-color: light-dark(rgb(168 85 247 / 30%), rgb(168 85 247 / 40%));
    box-shadow:
      0 12px 40px light-dark(rgb(168 85 247 / 15%), rgb(0 0 0 / 30%)),
      0 0 0 1px light-dark(rgb(255 255 255 / 80%), rgb(255 255 255 / 8%)) inset;
  }

  /* Card Header */
  .card-header {
    display: flex;
    gap: 1rem;
    align-items: center;
    flex-wrap: wrap;
  }

  .avatar {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    border-radius: 50%;
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    box-shadow: 0 4px 16px rgb(168 85 247 / 25%);
  }

  .avatar svg {
    width: 32px;
    height: 32px;
    color: white;
  }

  .client-info {
    flex: 1;
    min-width: 150px;
  }

  .client-name {
    margin-bottom: 0.25rem;
    font-family: Poppins, sans-serif;
    font-size: 1.125rem;
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .client-business {
    font-size: 0.875rem;
    color: light-dark(#64748b, #94a3b8);
  }

  .rating {
    font-size: 1.125rem;
  }

  /* Testimonial Text */
  .testimonial-text {
    margin: 0;
    font-size: 1.125rem;
    font-style: italic;
    line-height: 1.7;
    color: light-dark(#475569, #cbd5e1);
  }

  /* Result Badge */
  .result-badge {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    padding: 0.75rem 1.25rem;
    border-radius: 9999px;
    font-size: 0.9rem;
    font-weight: 700;
    color: white;
    background: linear-gradient(135deg, #10b981, #059669);
    box-shadow: 0 4px 16px rgb(16 185 129 / 30%);
  }

  .result-badge svg {
    width: 18px;
    height: 18px;
  }

  /* Stats Row */
  .stats-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    padding: 2.5rem;
    border: 2px solid light-dark(rgb(168 85 247 / 20%), rgb(168 85 247 / 30%));
    border-radius: 1.5rem;
    text-align: center;
    background: light-dark(rgb(255 255 255 / 80%), rgb(30 27 75 / 40%));
    backdrop-filter: blur(10px);
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.3s forwards;
  }

  .stat-item {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .stat-number {
    font-family: Poppins, sans-serif;
    font-size: 2.5rem;
    font-weight: 800;
    background: linear-gradient(135deg, #a855f7, #06b6d4);
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .stat-label {
    font-size: 0.95rem;
    font-weight: 600;
    color: light-dark(#64748b, #94a3b8);
  }

  /* Responsive */
  @media (width >= 768px) {
    .testimonials-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .testimonial-card.featured {
      grid-column: 1 / -1;
    }
  }

  @media (width >= 1024px) {
    .testimonials-grid {
      grid-template-columns: repeat(3, 1fr);
    }

    .testimonial-card.featured {
      grid-column: auto;
    }
  }

  @media (width <= 767px) {
    .testimonials-section {
      padding: 5rem 0;
    }

    .testimonials-header {
      margin-bottom: 2.5rem;
    }

    .testimonial-card {
      padding: 2rem 1.5rem;
    }

    .stat-number {
      font-size: 2rem;
    }

    .stats-row {
      padding: 2rem 1.5rem;
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      transform: translateY(30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes blobFloat {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(-25px, -40px) scale(1.08);
    }
    66% {
      transform: translate(30px, 25px) scale(0.92);
    }
  }

  @keyframes orbPulse {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.12;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.15);
      opacity: 0.18;
    }
  }
</style>
