---
// Problem-Agitate-Solution component
---

<section class="problem-agitate content-grid">
  <!-- Background Elements -->
  <div class="problem-background full-width" aria-hidden="true">
    <div class="floating-blob blob-1"></div>
    <div class="floating-blob blob-2"></div>
    <div class="gradient-orb"></div>
  </div>

  <div class="problem-container">
    <!-- Problem Header -->
    <div class="problem-header">
      <div class="problem-badge">
        <svg
          class="badge-icon"
          width="16"
          height="16"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <circle cx="12" cy="12" r="10"></circle>
          <line x1="12" y1="8" x2="12" y2="12"></line>
          <line x1="12" y1="16" x2="12.01" y2="16"></line>
        </svg>
        <span>El Problema</span>
      </div>
      <h2 class="problem-title">
        ¿Tu web actual no genera
        <span class="gradient-text">los clientes que esperabas?</span>
      </h2>
    </div>

    <!-- Problems Grid -->
    <div class="problems-grid">
      <!-- Problem 1 -->
      <div class="problem-card">
        <div class="problem-icon problem-red" aria-hidden="true">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="15" y1="9" x2="9" y2="15"></line>
            <line x1="9" y1="9" x2="15" y2="15"></line>
          </svg>
        </div>
        <h3>Tienes visitas pero nadie compra</h3>
        <p>
          Tu web recibe tráfico pero las conversiones son decepcionantes. Los
          visitantes llegan, miran y se van sin dejar datos ni comprar.
        </p>
      </div>

      <!-- Problem 2 -->
      <div class="problem-card">
        <div class="problem-icon problem-red" aria-hidden="true">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="15" y1="9" x2="9" y2="15"></line>
            <line x1="9" y1="9" x2="15" y2="15"></line>
          </svg>
        </div>
        <h3>Tu competencia aparece primero en Google</h3>
        <p>
          Mientras ellos capturan clientes desde búsquedas orgánicas, tú
          dependes de publicidad pagada que drena tu presupuesto.
        </p>
      </div>

      <!-- Problem 3 -->
      <div class="problem-card">
        <div class="problem-icon problem-red" aria-hidden="true">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="15" y1="9" x2="9" y2="15"></line>
            <line x1="9" y1="9" x2="15" y2="15"></line>
          </svg>
        </div>
        <h3>Tu diseño se ve amateur y desactualizado</h3>
        <p>
          Tu web no refleja la calidad de tus servicios. Los visitantes dudan
          de tu profesionalismo antes siquiera de leer tu propuesta.
        </p>
      </div>

      <!-- Problem 4 -->
      <div class="problem-card">
        <div class="problem-icon problem-red" aria-hidden="true">
          <svg
            width="24"
            height="24"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
          >
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="15" y1="9" x2="9" y2="15"></line>
            <line x1="9" y1="9" x2="15" y2="15"></line>
          </svg>
        </div>
        <h3>No sabes si tu inversión está funcionando</h3>
        <p>
          Sin métricas claras ni análisis, navegas a ciegas. No sabes qué
          mejorar ni dónde invertir para crecer.
        </p>
      </div>
    </div>

    <!-- Agitation -->
    <div class="agitation-box">
      <div class="agitation-content">
        <p class="agitation-text">
          <strong>No estás solo.</strong> El 78% de emprendedores pierde dinero
          y oportunidades porque su web no transmite confianza profesional ni
          está optimizada para convertir.
        </p>
      </div>
    </div>

    <!-- Solution -->
    <div class="solution-box">
      <div class="solution-icon" aria-hidden="true">
        <svg
          width="40"
          height="40"
          viewBox="0 0 24 24"
          fill="none"
          stroke="currentColor"
          stroke-width="2"
        >
          <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
          <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
      </div>
      <h3 class="solution-title">La Solución</h3>
      <p class="solution-text">
        Creo sitios web que <strong>convierten visitantes en clientes fieles</strong
        >. Diseño estratégico + Desarrollo optimizado + SEO que posiciona =
        Más ventas sin depender de publicidad.
      </p>
      <ul class="solution-benefits">
        <li>
          <svg
            class="check-icon"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
          >
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          Diseño profesional que transmite confianza desde el primer segundo
        </li>
        <li>
          <svg
            class="check-icon"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
          >
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          SEO optimizado para aparecer en las primeras posiciones de Google
        </li>
        <li>
          <svg
            class="check-icon"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
          >
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          Páginas de alta conversión que transforman visitas en ventas
        </li>
        <li>
          <svg
            class="check-icon"
            width="20"
            height="20"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
          >
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          Analítica clara para tomar decisiones basadas en datos reales
        </li>
      </ul>
    </div>
  </div>
</section>

<style>
  /* Main Section */
  .problem-agitate {
    position: relative;
    overflow: hidden;
    padding: 8rem 0;
    background: light-dark(
      linear-gradient(to bottom, #fef2f2, #fef3c7, #ecfeff),
      linear-gradient(to bottom, #1e1b4b, #0f172a, #042f2e)
    );
  }

  /* Background Elements */
  .problem-background {
    pointer-events: none;
    position: absolute;
    z-index: 0;
    top: 0;
    left: 0;
    overflow: hidden;
    width: 100%;
    height: 100%;
  }

  .floating-blob {
    position: absolute;
    width: 35rem;
    height: 35rem;
    border-radius: 50%;
    opacity: 0.08;
    filter: blur(100px);
    animation: blobFloat 20s ease-in-out infinite;
  }

  .blob-1 {
    top: -10%;
    left: -10%;
    background: linear-gradient(135deg, #ef4444, #dc2626);
    animation-delay: 0s;
  }

  .blob-2 {
    right: -10%;
    bottom: -10%;
    background: linear-gradient(135deg, #10b981, #059669);
    animation-delay: 10s;
  }

  .gradient-orb {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 25rem;
    height: 25rem;
    border-radius: 50%;
    opacity: 0.1;
    background: radial-gradient(circle, #f59e0b, transparent);
    filter: blur(80px);
    animation: orbPulse 15s ease-in-out infinite;
  }

  /* Container */
  .problem-container {
    position: relative;
    z-index: 1;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Header */
  .problem-header {
    margin-bottom: 4rem;
    text-align: center;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  .problem-badge {
    display: inline-flex;
    gap: 0.5rem;
    align-items: center;
    margin-bottom: 1.5rem;
    padding: 0.6rem 1.2rem;
    border: 1px solid light-dark(rgb(239 68 68 / 30%), rgb(239 68 68 / 40%));
    border-radius: 2rem;
    font-size: 0.9rem;
    font-weight: 600;
    color: light-dark(#dc2626, #fca5a5);
    background: light-dark(rgb(239 68 68 / 10%), rgb(239 68 68 / 15%));
  }

  .badge-icon {
    width: 1rem;
    height: 1rem;
  }

  .problem-title {
    font-family: Poppins, sans-serif;
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    line-height: 1.2;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .gradient-text {
    background: linear-gradient(135deg, #ef4444, #dc2626, #b91c1c);
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  /* Problems Grid */
  .problems-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
    margin-bottom: 3rem;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.2s forwards;
  }

  .problem-card {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 2rem;
    border: 2px solid light-dark(rgb(239 68 68 / 15%), rgb(239 68 68 / 25%));
    border-radius: 1.25rem;
    background: light-dark(rgb(255 255 255 / 90%), rgb(30 27 75 / 50%));
    backdrop-filter: blur(10px);
    box-shadow:
      0 4px 20px light-dark(rgb(239 68 68 / 8%), rgb(0 0 0 / 20%)),
      0 0 0 1px light-dark(rgb(255 255 255 / 60%), rgb(255 255 255 / 5%)) inset;
    transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .problem-card:hover {
    transform: translateY(-5px);
    border-color: light-dark(rgb(239 68 68 / 30%), rgb(239 68 68 / 40%));
    box-shadow:
      0 8px 30px light-dark(rgb(239 68 68 / 15%), rgb(0 0 0 / 30%)),
      0 0 0 1px light-dark(rgb(255 255 255 / 80%), rgb(255 255 255 / 8%)) inset;
  }

  .problem-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    border-radius: 0.75rem;
  }

  .problem-red {
    background: linear-gradient(135deg, #ef4444, #dc2626);
    color: white;
    box-shadow: 0 4px 16px rgb(239 68 68 / 25%);
  }

  .problem-card h3 {
    font-family: Poppins, sans-serif;
    font-size: 1.25rem;
    font-weight: 700;
    color: light-dark(#1e1b4b, #f8fafc);
  }

  .problem-card p {
    font-size: 1rem;
    line-height: 1.7;
    color: light-dark(#64748b, #cbd5e1);
  }

  /* Agitation Box */
  .agitation-box {
    margin-bottom: 3rem;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.3s forwards;
  }

  .agitation-content {
    padding: 2rem 2.5rem;
    border-left: 4px solid #f59e0b;
    border-radius: 0.75rem;
    background: light-dark(
      linear-gradient(135deg, rgb(251 191 36 / 15%), rgb(245 158 11 / 10%)),
      linear-gradient(135deg, rgb(180 83 9 / 20%), rgb(120 53 15 / 15%))
    );
    backdrop-filter: blur(10px);
  }

  .agitation-text {
    font-size: 1.15rem;
    line-height: 1.7;
    color: light-dark(#78350f, #fde68a);
  }

  .agitation-text strong {
    font-weight: 700;
    color: light-dark(#92400e, #fbbf24);
  }

  /* Solution Box */
  .solution-box {
    padding: 3rem 2.5rem;
    border: 2px solid light-dark(rgb(16 185 129 / 25%), rgb(16 185 129 / 35%));
    border-radius: 1.5rem;
    text-align: center;
    background: light-dark(rgb(255 255 255 / 90%), rgb(30 27 75 / 50%));
    backdrop-filter: blur(10px);
    box-shadow:
      0 10px 40px light-dark(rgb(16 185 129 / 12%), rgb(0 0 0 / 25%)),
      0 0 0 1px light-dark(rgb(255 255 255 / 60%), rgb(255 255 255 / 5%)) inset;
    opacity: 0;
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) 0.4s forwards;
  }

  .solution-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 72px;
    height: 72px;
    margin-bottom: 1.5rem;
    border-radius: 50%;
    background: linear-gradient(135deg, #10b981, #059669);
    box-shadow: 0 8px 25px rgb(16 185 129 / 30%);
  }

  .solution-icon svg {
    color: white;
  }

  .solution-title {
    margin-bottom: 1rem;
    font-family: Poppins, sans-serif;
    font-size: 1.75rem;
    font-weight: 800;
    color: light-dark(#065f46, #6ee7b7);
  }

  .solution-text {
    max-width: 700px;
    margin: 0 auto 2rem;
    font-size: 1.15rem;
    line-height: 1.7;
    color: light-dark(#475569, #cbd5e1);
  }

  .solution-text strong {
    font-weight: 700;
    color: light-dark(#065f46, #6ee7b7);
  }

  .solution-benefits {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: 650px;
    margin: 0 auto;
    padding: 0;
    list-style: none;
    text-align: left;
  }

  .solution-benefits li {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    font-size: 1.05rem;
    line-height: 1.6;
    color: light-dark(#1e293b, #e5e7eb);
  }

  .check-icon {
    flex-shrink: 0;
    margin-top: 0.125rem;
    color: #10b981;
  }

  /* Responsive */
  @media (width >= 768px) {
    .problems-grid {
      grid-template-columns: repeat(2, 1fr);
      gap: 2rem;
    }
  }

  @media (width <= 767px) {
    .problem-agitate {
      padding: 5rem 0;
    }

    .problem-header {
      margin-bottom: 2.5rem;
    }

    .problem-card {
      padding: 1.5rem;
    }

    .agitation-content {
      padding: 1.5rem;
    }

    .agitation-text {
      font-size: 1rem;
    }

    .solution-box {
      padding: 2rem 1.5rem;
    }

    .solution-text {
      font-size: 1rem;
    }

    .solution-benefits li {
      font-size: 0.95rem;
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      transition-duration: 0.01ms !important;
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
    }
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      transform: translateY(30px);
      opacity: 0;
    }
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes blobFloat {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
    }
    33% {
      transform: translate(-25px, -40px) scale(1.08);
    }
    66% {
      transform: translate(30px, 25px) scale(0.92);
    }
  }

  @keyframes orbPulse {
    0%,
    100% {
      transform: translate(-50%, -50%) scale(1);
      opacity: 0.1;
    }
    50% {
      transform: translate(-50%, -50%) scale(1.15);
      opacity: 0.15;
    }
  }
</style>
