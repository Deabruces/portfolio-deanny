---
import Bombilla from "../assets/icons/bulb.svg";
import Hands from "../assets/icons/hands.svg";
import Web from "../assets/icons/www.svg";
---

<section class="content-grid">
  <div class="features">
    <div class="card">
      <div class="icon">
        <!-- Bombilla (idea) -->
        <Bombilla />
      </div>

      <h3>Transformo tus ideas</h3>
      <p>
        De la inspiración a la acción, convierto tus conceptos en proyectos
        digitales reales.
      </p>
    </div>

    <div class="card">
      <div class="icon">
        <!-- Globo terráqueo (digital) -->
        <Web />
      </div>
      <h3>En experiencias digitales</h3>
      <p>Diseño páginas web con UX/UI optimizadas para conectar y vender.</p>
    </div>

    <div class="card">
      <div class="icon">
        <!-- Manos (conectar) -->
        <Hands />
      </div>
      <h3>Que conectan y venden</h3>
      <p>
        Integro estrategias digitales que generan confianza y resultados
        medibles.
      </p>
    </div>
  </div>
</section>
<style>
  .features {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    padding: 4rem 2rem;
  }

  .card {
    position: relative;

    overflow: hidden;

    padding: 2rem;
    border-radius: 1rem;

    text-align: center;

    box-shadow: 0 4px 15px rgb(0 0 0 / 8%);

    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  .card::before {
    pointer-events: none;
    content: "";

    position: absolute;
    z-index: 1;
    inset: 0;

    border: 6px solid transparent;
    border-radius: 1rem;

    /* Create a gradient border using two backgrounds and background-clip */
    background:
      linear-gradient(var(--deanny-bg-color), var(--deanny-bg-color))
        padding-box,
      linear-gradient(135deg, var(--deanny-purpura), var(--deanny-cian))
        border-box;
  }

  .card > * {
    position: relative;
    z-index: 2;
  }

  .card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 20px rgb(0 0 0 / 12%);
  }

  .icon {
    width: 50px;
    height: 50px;
    margin: 0 auto 1rem;
    color: var(--deanny-lila);
  }

  .icon svg {
    width: 100%;
    height: 100%;
  }

  .card h3 {
    margin-bottom: 0.75rem;
    font-family: Poppins, sans-serif;
    font-size: 1.25rem;
    color: var(--deanny-lila);
  }

  .card p {
    font-family: Inter, sans-serif;
    font-size: 1rem;
  }
</style>
